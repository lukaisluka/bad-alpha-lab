你正在为 Bad Alpha Lab 网站添加 BAL-003 策略复刻报告页面。

项目目录：~/Projects/bad-alpha-lab/site/
技术栈：Next.js 14 + TypeScript + Tailwind CSS

## 任务

### 1. 创建 BAL-003 组件
文件：`src/app/replications/[slug]/bal-003.tsx`

内容来源：`~/Projects/bad-alpha-lab/content/replications/BAL-003-market-making-full.md`

按照已有的 bal-001.tsx 和 bal-002.tsx 的风格，把 markdown 内容转化为 React 组件。

关键样式规则：
- 外层 div: `max-w-4xl mx-auto px-6 py-16 md:py-20`
- 章节标题: `section-label` class
- 引用块: `bg-lab-dark border border-lab-border rounded-lg p-6`
- 红色强调: `text-lab-red font-semibold` 或 `text-lab-red font-bold`
- 表格: lab 风格（参考 bal-001.tsx 的表格样式）
- 列表: `space-y-2 text-lab-gray`
- 结论框: `bg-lab-dark border border-lab-red/50 rounded-lg p-6`
- 免责声明: `border-t border-lab-border pt-8`

### 2. 更新动态路由
文件：`src/app/replications/[slug]/page.tsx`

在 replications 数组中添加 BAL-003：
```typescript
import BAL003 from './bal-003'
// ...
{ slug: 'bal-003', title: '做市商策略：spread capture 的幻觉', component: BAL003 }
```

同时更新 `generateStaticParams` 返回值。

### 3. 更新复刻列表页
文件：`src/app/replications/page.tsx`

添加 BAL-003 的卡片到列表中。

### 4. 更新首页尸检卡
文件：`src/app/page.tsx`

把第三张尸检卡（加密货币均值回归）改为指向 `/replications/bal-003`，内容改为做市商策略相关。

请直接开发，不要询问。